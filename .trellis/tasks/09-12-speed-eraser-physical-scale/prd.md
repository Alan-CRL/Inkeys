# 笔速橡皮：持续清扫与鼠标定位交互（2026-09-13）

## Baseline and scope
基于feature/eraser@c433b6f5c34cde5e3286f3beaf6a433a5fabc623继续。主会话独立完成所有工作，不调用子Agent，不切分支、不覆盖本地修改、不提交或归档。保留显示物理有效性、动作/覆盖尺度分离、原始QPC、历史压缩、轨迹处理及已有文档几何。

## Current behavior
- Dstandard明确等于既有Dmin，Dmax和两设备模式的cm/DIP覆盖范围不变。
- 鼠标Hover只定位；真正MouseLeft/MouseRight Down清空旧运动/证据/收尾状态。
- Down后短时提速不绕过阻力，持续真实运动按时间累积有界证据，再渐进扩大。帧、静止、预测、补点、连接不提供新的放大证据。
- 保持基于实际较大尺寸及已建立清扫状态，短停/折返稳定，持续慢擦后平滑回小。保持与确认并行。
- 真实鼠标Up立即退出逻辑清扫，约140ms非擦除视觉收尾从已接受直径收缩，无新移动也能完成。再Down立即安全起步。
- Pen保留既有Hover交接，Touch保留新接触最小及位移解锁；真正重连不计合成位移。修复倒转笔恢复工具编号遗漏。
- Contact光标、真正擦除半径、烘干/历史一致；显示版本按活动批次锁存。配置/模式切换、取消和并发lane不遗留旧意图。

## Acceptance
- 先补回归：50/120ms短快划不超过标准1.25倍；旧实现应失败。
- 持续快擦350ms有可见扩张，800ms至少65%Dmax、1s至少85%Dmax；中速连续折返保持对应中间值。
- 大尺寸280/500ms短停不超过10%下降，持续慢擦550ms仍保持、900ms开始明显缩小，之后收敛。
- 高速Hover数秒不扩大或影响后续静止Down；Up20ms内再Down不继承历史/残余轮廓。
- 取消、初始化失败、共享左右键所有权、迟到Hover/Up、重复时间、无移动收尾、延迟呈现、配置切换和真正断触分别覆盖。
- 保留两模式、物理与DIP、96/144/192DPI、60/125/240/1000Hz及不同帧调度、5%尺寸一致性和原有范围/Touch/重连测试。
- 完整InkeysRepo.sln Debug|ARM64、InkeysHeadlessTests.exe --no-window、差异/编码检查通过。真实设备/GUI/Win7体验如未执行须明确记录。
