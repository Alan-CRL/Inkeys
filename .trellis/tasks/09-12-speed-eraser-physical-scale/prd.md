# 橡皮DIP尺寸与无事件静止回缩

## 基线
feature/eraser@7af954bb17bd4e4b8e3b76d009ab11e939160c17。原有PptCOM.dll改动保留。全部由主Agent完成，不委派，不切分支，不提交或归档。

## 本轮规格
- 所有输入、EDID状态及设备模式共用明确DIP尺寸：最小16、标准32、最大160、Touch起步16。固定模式50DIP旁路，不运行速度控制器。
- EDID/物理信息只用于动作解释和后续能力；保留第一阶段有效性、映射和显示批次冻结。
- 笔电DIP清扫进入/退出/大目标速度800/600/1900；大屏DIP650/450/1700；直接Touch25/18/70cm/s。普通速度长擦不积累成清扫。
- 当前工具有效尺寸与历史半径分离。最后有效移动280ms后可见回缩，Mouse/Pen回标准32、未解锁Touch保持16；不用同位置包刷新“有效移动”时钟。
- 静止不产生几何，恢复时追加同位尺寸断点，旧点不可修改；新足迹不能带旧粗尾，原保存和回放格式继续使用。
- 维持Mouse Hover/Down/Up、140ms收尾、Pen交接、Touch起步、真重连、并发所有权和显示版本。
- 复用原输入/MPMC/模型/轨迹/渲染；只调整单位、尺寸状态、必要接入及可关闭的诊断/测试。

## 验收
- 尺寸不受EDID、物理尺寸、模式影响，DPI换算正确；固定旁路独立。
- 普通长擦20秒、速度扫描、短快划和持续快擦符合资格；时序采样一致性保留5%界限。
- 真正无Move时产品最终接触光标缩小、历史点数不变；收敛后停帧，短Move恢复足迹无粗尾。
- 实际尺寸断点经UInk导出、文件保存/读取、导入保持，并支持真实Undo/Redo。
- 完整ARM64方案构建、无窗口测试及专项隐藏测试；旧全量隐藏套件未通过项必须单列。
