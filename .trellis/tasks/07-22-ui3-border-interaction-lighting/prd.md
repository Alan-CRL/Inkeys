# UI3 边框点光追光效果

## Goal

为 UI3 Bar 的指定 1px 圆角矩形和超椭圆边框提供平滑的三点光追光反馈：保留主按钮中心常驻光，延长点击/拖动残光，并增加不受窗口边界影响的全局鼠标光，同时继续保留可供其他控件使用的原纯色边框绘制方式。

## Background

- UI3 Bar 已使用 `ID2D1DeviceContext`、WARP、预乘 Alpha 分层窗口和脏区更新。
- 当前主按钮、主栏、绘制属性栏以及 11 个颜色块均有 1px 边框；颜色块需保持上一任务确定的尺寸、圆角、间距和柔和主题灰色。
- 鼠标与触摸最终都进入 `BarUISetClass::Interact` 的客户区消息路径，主按钮拖动由 `Seek` 处理。
- 首版双光源已经完成并通过人工视觉检查；维护者反馈 450ms 的交互退场和 100ms 主光变暗过快，且窗口外鼠标位置不能连续影响边框。

## Requirements

- 仅修改 UI3 Bar，不修改传统 `IdtFloating`，不新增配置、新线程或独立动画系统。
- 圆角矩形和超椭圆提供 `Solid`、`PointLight` 两种内部边框绘制模式，默认保持 `Solid`，现有构造函数及外部接口不变。
- 主按钮、主栏、绘制属性栏外框和 11 个颜色块显式使用 `PointLight`；其他边框继续使用原纯色路径。
- 同时最多存在三个光源：跟随主按钮当前中心的常驻光源、最近点击/滑动位置的交互残光，以及实时全局鼠标光。
- 光照半径为 480 逻辑像素；使用边框自身主题色和 `framePct`，不改变任何填充。
- 每个光源绘制 3px 微弱扩散层和原 1px 清晰层；扩散层强度为清晰层的 12%。
- 交互残光最大强度为 40%，在最后一次位置更新后以 smoothstep 于 1× 动画速度下 1.8 秒内衰减至 0。
- 主光脉冲在交互开始后前 300ms 由 100% 降至 60%，随后在 1.5 秒内恢复；两段过程均受现有动画速度倍率控制。
- 主按钮拖动持续更新交互光位置但不重启主光脉冲；重复按下创建新的交互过程。
- 全局鼠标光最大强度为 60%，位置严格跟随系统鼠标；鼠标从窗口外或其他显示器移近时依靠 480px 空间衰减连续变亮，不增加位置缓动。
- UI 动画从关闭切换到开启时，鼠标光按当前速度倍率淡入 300ms；移动不会重复启动淡入。
- UI 动画关闭时立即移除交互残光、鼠标光和主光脉冲，只保留强度稳定的主光；重新开启不得恢复旧交互残光。
- 使用 UI3 窗口的 `RIDEV_INPUTSINK` Raw Input 接收全局鼠标移动，不复用或修改传统 `IdtFloating` 的低级鼠标钩子。
- 非穿透绘制模式（`StateModeSelect == IdtPen && !penetrate.select`）下，第一主光对所有 PointLight 边框使用动画中的当前画笔色；其他光源继续使用边框自身颜色并允许 `SOURCE_OVER` 自然混合。
- 鼠标静止且其他光影动画结束后补绘最终状态并恢复现有渲染休眠。
- D2D 渐变资源失败时回退到纯色边框并避免逐帧重复日志。
- Raw Input 注册失败时只记录一次错误并禁用第三光源，不影响主光、交互光和纯色回退。
- 光影外扩必须进入脏区，移动、展开、淡出后不得留下残影。
- 保留文件原编码、CRLF、现有格式，并为关键状态和兼容回退添加简短中文注释。

## Acceptance Criteria

- [ ] `Solid` 是默认模式，原 `DrawRoundedRectangle` / `DrawGeometry` 纯色边框仍可用。
- [ ] 仅主按钮、主栏、绘制属性栏外框和 11 个颜色块显式启用追光。
- [ ] 常驻光以主按钮当前中心为准；交互残光按 1.8 秒退场，鼠标光在窗口外移动时仍能实时更新。
- [ ] 主按钮、主栏按钮、颜色、画笔和荧光笔的左键/触摸按下均能触发交互光。
- [ ] 动画关闭时只保留稳定主光；开启时鼠标光淡入，动画速度倍率同步控制残光和主光脉冲。
- [ ] 非穿透绘制模式下主光使用当前画笔色，并能与鼠标光、交互光的边框色自然混合；橡皮、穿透及其他模式恢复边框自身颜色。
- [ ] 光影只影响边框，深浅主题、颜色块主题灰、填充、布局、命中与原动画行为保持不变。
- [ ] 鼠标静止且光影动画结束后渲染线程恢复原静止等待，且柔光脏区无残影。
- [ ] `git diff --check` 通过，完整 `InkeysRepo.sln` 的 `Debug | ARM64` 后台构建通过。
- [ ] 不启动程序、不打开窗口、不执行屏幕操作；不提交 commit、不归档任务，等待人工验收。

## Out of Scope

- 传统悬浮栏、设置窗口和画板渲染。
- 右键关闭、触摸的全局跟随光或无屏幕鼠标坐标的程序化指针变化。
- Gaussian Blur、DirectComposition、配置化视觉参数和自动化截图对比。
