# 本轮验证记录

2026-09-06，限规划资料与独立审查稿。产品代码未修改，任务保持 planning。

## 已执行

- 读取旧迁移任务、主要迁移提交以及 Setting/Widgets/Layout/字体装载/STB loader 的代码证据。
- 通过只读脚本读取 4 个字体 head/hhea 表；记录同一 1.213 换算比例。
- Browser 检查 1200×840、960×700、720×520；导出相应截图并目视检查代表稿。
- 额外检查 360×640 内容预算重排：9 个设置行的 action 均在行范围内，文字/操作矩形无相交，document 无水平溢出。这不等同于原生 DPI 或触摸测试。
- 宽窗全部 9 行无文字/action 重叠；普通双行卡片实际 72 高。普通行 action 到外边框 17 像素（包含 1 边框），即到内边缘 16；details 无内层边框时为 16。
- 默认窗与最小窗无水平溢出；最小窗导航展开后 pane=248，content.x=48 不变。
- 导航图标中心 x=24，菜单按钮 x=4；展开文字和 pane 标题 x=48。收起保留分组占位，使图标纵向位置也不因隐藏标题跳变。
- 开关切换后 aria-checked 正确变化；主栏 slider 键盘加 1 后实际值和输出均为 101%；置顶详情展开后 selector 可见，改选「较短（1 秒）」后摘要同步。
- 插件列表进入 PPT 子页，breadcrumb 返回插件列表有效。
- 审查稿脚本通过 Node VM 语法检查，浏览器 error/warn 日志为空。
- 新任务的 prd.md 和 task.json 保持生成时的 UTF-8 无 BOM / CRLF；新增文本文件采用 UTF-8。

## 未执行 / 不应混淆

- 未构建产品 Solution，未运行原生 headless tests：本轮没有产品实现。
- 未启动 Inkeys 或 Office 窗口，未验证原生字形渲染、窗口交互、DWM、深色、动画、触摸或动态脏区。
- 预览的配置、外链和系统动作仅为局部演示，不是后端绑定测试。
- 未将全部页面绘制成效果稿；已完成路由级迁移清单，逐 settingId 的绑定覆盖表属于实现前首步。
- 未 commit/push。只新增本任务目录中的规划与审查资产。
