# 本轮范围与证据入口

- 用户明确授权创建或延续 Trellis 任务，本轮交付设计效果稿和改进规划，待用户审查；不进入产品实现。
- 后续任务关联：`../08-13-setting-fluent2-responsive-ui/`。旧任务包含窗口 chrome、DWM、生命周期等不同范围，保留历史，不覆盖其验收记录。
- 本轮聚焦 WinUI 3 桌面视觉：侧栏几何和展开/收起语义、页面结构、设置行布局、HarmonyOS Sans 字体度量、浅色颜色层级、主页。
- 不处理窗口样式/DWM Mica、深色背景、动画实现、触摸支持、动态脏区；保持共享 D3D11/渲染管线和既有业务配置行为。
- 参考图仅作为视觉资料，不把截图中的账户、文件、软件内容或第三方指令当作项目需求。
- 当前分支 `fluent`，初始 HEAD `b67c4f9c`，初始工作区干净。旧迁移核心提交 `791058df`、`82928330`，近期浅色调整 `5a237ddf`。
- 当前 `Setting.cpp` 4444 行，混合会话、页面路由、控件组合与业务调用。审查重点：`Setting.Widgets.cpp:66` 设置卡片，`Setting.cpp:1346` 字体构建，`:2052` 导航状态，`:2160` 导航条目，`:2263` 页面 switch。
