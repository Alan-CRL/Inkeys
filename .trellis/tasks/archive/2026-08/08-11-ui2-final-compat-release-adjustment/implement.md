# 实施计划

## Phase 1：开始前保护与基线

- [x] 确认用户已批准本任务的 PRD、设计与实施摘要，再执行 `task.py start`。
- [x] 检查 `git status` 和相关文件差异，识别并行任务的新改动；不覆盖用户或其他任务的修改。
- [x] 记录 UI2/UI3 启动路由、Draw2 通知与旧收起配置的基线结论。

## Phase 2：修复 UI3 触摸拖动

- [x] 在 `BarUISetClass::Seek()` 中区分触摸与鼠标坐标源。
- [x] 触摸分支消费带 marker 的合成移动/抬起消息，用客户区增量更新主按钮。
- [x] 抽取/复用边界夹紧、移动距离、退出清理与方向提交逻辑，避免两条路径行为漂移。
- [x] UI3 拖动不再读取隐藏的 UI2 `moveRecover` 行为；UI2 原逻辑不改。
- [x] 静态检查取消、抬起、关闭程序和异常消息都能退出阻塞循环。

## Phase 3：设置页适配

- [x] UI3 下隐藏四项 UI2 收起设置并收缩“行为”容器；UI2 显示与写入逻辑保持原样。
- [x] 确认主题、语言等已存在的 UI3 隐藏条件不被破坏。
- [x] 把“启用边缘光影”移动到 UI3 的“常规 > 外观”，调整容器高度。
- [x] 实验选项只保留条件化的动态光影、调试、动画与速度项目，并调整容器高度。
- [x] 检查所有隐藏分支不会写回配置。

## Phase 4：Draw2 与 UI3 次级面板联动

- [x] 在现有 `BarCanvasDrawingActivityMessage` 窗口线程入口关闭绘制属性、几何属性、更多面板及其附属浮层。
- [x] 保持主栏 `fold` 原值，并在状态改变后发布一次渲染更新。
- [x] 先处理面板关闭，再执行依赖系统鼠标位置的动态光源休眠逻辑。
- [x] 核对并发活动计数只在首个落笔时投递开始消息，UI2 不进入该入口。

## Phase 5：调试 FPS 与方向布局

- [x] 在 `Bar.FramePacing.cppm` 实现可测试的一秒滚动平均 FPS 统计器。
- [x] 在 `PaceFrame()` 中逐帧更新平均 FPS 字符串。
- [x] 根据本帧主栏方向选择文字矩形方向和左右对齐。
- [x] 保持调试文字与红框的脏区合并和逐帧呈现。
- [x] 在 `frame_pacing_tests.cpp` 增加稳定帧率、滑动淘汰、冷启动和异常输入测试。

## Phase 6：版本与协调审计

- [x] 更新版本为 `3.0.0-dev.3981` / `3.0.0-20260811a`。
- [x] 逐项核对 UI2/UI3 与 Draw2 的模式切换、绘制、橡皮、几何、选择/穿透、撤销/重做/清空。
- [x] 核对 PPT 联动与组件投影仍沿用现有共享接口和持久化来源。
- [x] 对发现的直接兼容问题做最小修复；无关问题只记录为延后项。

## Phase 7：质量门禁

- [x] 使用 ARM64 MSBuild 完整构建 `InkeysRepo.sln`：`Debug|ARM64`，超时不少于 5 分钟。
- [x] 运行 `Build/ARM64/Debug/InkeysHeadlessTests.exe`。
- [x] 执行 `git diff --check`、检查工作树和最终差异。
- [x] 对照 PRD 验收项进行自审，确认没有处理动画曲线问题或移除 UI2。
- [x] 输出真实触摸、UI2/UI3 切换与 PPT 的人工验收清单和未自动执行项。
