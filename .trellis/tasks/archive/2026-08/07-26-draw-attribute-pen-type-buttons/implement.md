# 实施计划

1. 调整绘制属性 Shape/SVG/Word 枚举与状态，增加禁用的激光笔、刷子对象，删除旧滑槽对象和选择进度状态。
2. 新增原创刷子、激光笔 SVG，并在 `Inkeys.rc` 中登记；复用现有荧光笔 SVG。
3. 将绘制属性面板增高，重新计算紧凑态比例、左侧颜色/粗细纵向位置和右侧四按钮布局。
4. 抽取四按钮共同的布局、主题色和透明度设置，画笔/荧光笔保留选择逻辑，激光笔/刷子固定禁用态。
5. 将按钮渲染改为直接继承绘制属性面板，移除旧滑槽绘制及动态滑块几何。
6. 为可用按钮接入主栏同款悬停、按压缩放与释放回弹；确保禁用按钮不进入输入命中。
7. 扩展统一动画批次、上下换边关键帧和脏区循环到四个按钮，并核对收起时等比缩放。
8. 执行静态验证：
   - `rg` 确认旧 `DrawSelect`、`DrawSelectGroove`、`drawAttributePenSelectProgress` 无残留。
   - `git diff --check` 检查空白错误。
   - 检查改动文件仍为原有 CRLF；SVG 保持原资源风格。
   - 阅读完整 diff，逐项映射 PRD 验收标准。
9. 使用 ARM64 版 MSBuild 编译完整 `InkeysRepo.sln` 的 `Debug | ARM64`；修复 `IdtAtomic<double>` 条件表达式歧义后编译通过。

## 不执行

- 不启动程序，不进行运行时或手工 UI 测试。
