# 实施计划

1. 为 Shape 增加独立边框光影透明度，并让 `Shape()` 在本体隐藏时仍能绘制第三光源边框。
2. 在主栏布局计算中为非分割线按钮配置 PointLight 边框、默认颜色和半强度第三光源。
3. 在主栏布局状态计算中同步按钮颜色、显隐、按下变暗对应的 `frameLightPct`。
4. 在绘制属性画笔/荧光笔初始化与布局中配置同样的光影；隐藏 Brush2/Laser。
5. 确认按压缩放、颜色切换、动态光源门禁及 3px 漫反射脏区仍复用现有路径。
6. 执行 `git diff --check`、旧符号/资源静态检查和完整 `InkeysRepo.sln` 的 `Debug | ARM64` 编译。

## 最终用户验收与收尾

- 2026-08-09：用户明确确认本任务已完成人工核验，并授权结束、提交与归档。
- PRD 验收项据此记为 `PASS`；本节只记录用户验收，不改写既有实现和构建证据。
- 本轮未修改产品代码，未重复启动 GUI 或执行构建。
