# 统一主按钮 SVG 与绘制态颜色指示

## 目标与确认

用户人工测试后要求纠正 0606cbe0 的主按钮指示器，已确认理解并明确授权开始实施及创建跟进任务。当前工作树 D:/Project/Inkeys/Repo/Inkeys-draw、分支 theme，初始干净；保留 stash 7872c2c0 中原 draw 改动。

## 需求

- R1：深浅色使用同一个 SVG 基础结构及同一个主图标实例，按内部部分加工颜色/透明度；不用两套完整图片交叉淡化模拟变化。
- R2：深色样式恢复到主题改造前 4478887c 的效果：默认中性笔身与原有渐变颜色指示。不能继续使用新版本的深色整笔实色填充或额外提亮映射。
- R3：浅色绘制态保持整根笔使用真实笔色和仅外轮廓细描边；内部断口不描横向黑边。浅色屏幕保持中性色。
- R4：深浅过渡必须包含各部分颜色、旧渐变指示、浅色整笔填充与轮廓的连续变化，并接续现有主按钮材质动画；运行中换色和反向切换从 current 接续。
- R5：仅 IdtPen 与 IdtShape 显示颜色指示。Selection/Eraser 等非绘制模式下，两种主题都恢复各自默认中性样式，不显示记忆画笔色填充；显隐采用原有动画规则。图形使用 Brush1，活动激光/荧光/普通笔取各自真实颜色。
- R6：保留其余 Bar 配色、背景/边框/阴影/动态光、收展节奏、布局、普通图标、底部吸附、输入和持久化行为。

## 验收

- AC1：生产主按钮只消费一个共享 SVG，深浅仅改变内部属性；普通 SVG 几何不变。
- AC2：Dark 默认态及六种代表笔色绘制态与 4478887c 基线离屏对照；保留旧渐变，不是整笔实色。
- AC3：Light 绘制态整笔真实 RGB、外轮廓清楚、内部两处分节无横线，screen 不染笔色。
- AC4：Pen/Shape 与 Selection/Eraser 的两主题矩阵正确；非绘制态不依赖此前选择的笔色。
- AC5：主题/收展 0/.25/.5/.75/1、颜色改变、绘制态进入退出和中途反向连续；禁用动画到达一致端点，静止进入 idle，设备重建后完整恢复。
- AC6：完整 InkeysRepo.sln Debug|ARM64（ARM64 host MSBuild、超时至少五分钟）和全部 --no-window 测试通过；相关 SVG 静态/离屏验证通过。

## 限制

保持原编码/BOM/CRLF，关键逻辑写中文注释。只静态检查、编译和无窗口测试，不启动产品 GUI、不操作原 draw stash、不提交新的 commit。更新与本次要求冲突的当前主题规范，保留旧归档记录作为历史。
