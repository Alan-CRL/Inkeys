# Implementation Plan

1. 在简体中文基准中新增 Bar key，运行同步脚本并补齐英语、繁体中文翻译。
2. 修正启动语言选择，加载 SC/TC 字体资源，实现 Bar 本地化字体族与 locale 配置。
3. 将主栏按钮、绘制/几何/颜色面板、提示、错误和诊断文字替换为 i18n key。
4. 将标注提示辅助接口改为语义枚举并更新纯逻辑测试。
5. 用本地化测量结果完善颜色页脚、提示浮窗与 FPS 区域布局。
6. 扩展离屏测试，循环三种语言检查固定槽位和局部布局，并生成多 DPI/UI 缩放 PNG。
7. 运行 i18n 检查、静态审计、ARM64 构建、无窗口测试和离屏测试；检查 PNG、diff 与工作区状态。

## Constraints

- 最小化修改，不重构无关代码，不修改内置组件文案。
- 不手改生成头文件，不启动交互式 GUI，不提交或推送。
- 保持原文件编码与换行；不触碰既有 `Inkeys/PptCOM.dll` 修改。
